# Typing Coach Backend

Go API server for the Typing Coach platform.

## Run

```bash
go run ./cmd/server
```

Server runs on http://localhost:8080

## Endpoints

- `GET /` - API info
- `GET /api/health` - Health check
